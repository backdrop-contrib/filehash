<?php

/**
 * @file
 * Adds "1" to the end of every managed file.
 */

/**
 * Implements hook_file_validate().
 */
function filehash_test_file_validate($file) {
  file_put_contents($file->getFileUri(), '1', FILE_APPEND);
}
